/*
Given an array of integers, detemine whether the sum of its elements is even or odd. Return "even" or "odd".
If input array is empty return consider it  as arr = [0]

*/
function evenOrOdd(arr) {
    
} 

/*
My Solution:
const evenOrOdd =(arr =[0]) => {
    
    let initial = 0;
	const sumOfArray = arr.reduce((total, current) => {
	    return total + current;
	}, initial);
	
	return sumOfArray % 2 === 0 ? "even" : "odd";
}
*/
/*
Create a function that takes an array of numbers, a string and return
an array of numbers as per the following rules:
* "Asc" returns array in ascending order
* "Des" returns array in descending order
* "None" return original array
*/

function ascDesNone(arr, str) {
	
}

/*

My Solution:
 const ascDesNone = (arr, str) => {
	if(str === "Asc"){
	    return arr.sort((a, b) => {
	        return a-b;
	    })
	} else if(str === "Des"){
	    return arr.sort((a, b) => {
	        return b-a;
	    });
*/